{
  "name": "devils-table",
  "version": "0.2.0-alpha.7",
  "private": true,
  "type": "module",
  "description": "Development and validation tools for Devil's Table: Goods & Provisions.",
  "scripts": {
    "test": "node --test",
    "validate": "node tools/validate-data.mjs",
    "check": "npm run validate && npm test",
    "package": "npm run check && node tools/package.mjs"
  },
  "engines": { "node": ">=20" }
}
